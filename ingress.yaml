apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: go-ingress
spec:
  rules:
    - host: goviolin.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: go-violin-service
                port:
                  number: 8000
          